{
  "name": "ts-simple-monorepo",
  "scripts": {
    "postinstall": "run-s bootstrap",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "lint": "eslint .",
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "client:dev": "cd packages/greeter-client && npm run dev",
    "dev": "run-p watch client:dev",
    "test": "jest"
  },
  "devDependencies": {
    "@spa/linting": "./packages/linting",
    "@tsconfig/node16": "^1.0.2",
    "@types/jest": "^27.4.1",
    "@types/node": "^16.11.26",
    "jest": "^27.5.1",
    "lerna": "^3.22.1",
    "npm-run-all": "^4.1.5",
    "ts-jest": "^27.1.4",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.6.3"
  }
}
